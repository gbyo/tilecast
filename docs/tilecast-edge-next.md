# Tilecast Edge: state and next work

This document is the implementation ledger for Tilecast Edge 1. It records what exists, what is proven, what is missing, and how to continue without breaking the architecture.

Read in this order:

1. [`tilecast-edge.md`](tilecast-edge.md): the Edge 1 architecture. It is binding. If a change must deviate from it, stop and write the complete tradeoff first.
2. [`apps/edge/AGENTS.md`](../apps/edge/AGENTS.md): fixed decisions and review rules for the code.
3. [`apps/edge/README.md`](../apps/edge/README.md): crates, dependency direction, build and test commands.
4. This document.

Work that is not part of Edge 1 is in [`tilecast-edge-future.md`](tilecast-edge-future.md). Do not start it from here.

## 1. Current state

### 1.1 What exists and is proven

| Area                                    | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Proof                                                                                                                                                                                                                                     |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Contracts (`edge-protocol`)             | IDs, digests, time, bounded text, capability model, IPC v1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Unit tests; golden IPC fixtures in `packages/edge-protocol/fixtures/ipc` (`tests/fixtures.rs`).                                                                                                                                           |
| State (`edge-state`)                    | SQLite with embedded migrations, newer-schema refusal, integrity check after an unclean shutdown, typed repositories.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/state.rs`.                                                                                                                                                                                                                         |
| Daemon lifecycle                        | `Type=notify`, watchdog gated on a state query, recovery mode on state failure, clean shutdown with WAL checkpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tilecastd/tests/daemon.rs`.                                                                                                                                                                                                              |
| Local IPC (`edge-ipc`)                  | Versioned framing, handshake, UID policy, role checks, backpressure, renderer supersession.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `tests/ipc.rs` and a policy unit test.                                                                                                                                                                                                    |
| `tilecastctl`                           | `status`, `capabilities`, `cache`, `self-test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Used by both end-to-end scripts.                                                                                                                                                                                                          |
| WPE renderer                            | WPEPlatform (drm, wayland, headless), the shared Player Runtime through the `TilecastRuntimeHostV1` adapter, the `tcmedia` scheme and `tcmediasrc` GStreamer source.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `renderer-wpe/ci/run-e2e.sh`: status surface, daemon restart, renderer crash, image, H.264 video, transitions, render-tree widget, layout. Real WPE WebKit 2.54 in the headless image.                                                    |
| Player Runtime                          | `@tilecast/player-runtime`: Lit views, XState playback machines, the monotonic scheduler and shared timeline, `MediaSurface` implementations and Web Animations transitions. Electron and WPE host the same artifact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `npm test --workspace @tilecast/player-runtime`; the cross-engine conformance suite (15 fixtures, Electron and WPE identical); parity and performance against the legacy renderer ([`player-runtime.md`](player-runtime.md) §8–9).        |
| Supervision                             | Evidence-based recovery ladder, safe mode, incompatible-presentation surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `supervisor.rs` unit tests; headless crash scenarios.                                                                                                                                                                                     |
| Content store (`edge-cas`)              | Verified commit order, crash reconciliation, one writer per digest, pins, LRU by domain, free-space reserve, multi-source fetch with resume and integrity fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tests/cas.rs`.                                                                                                                                                                                                                           |
| Server client (`edge-server`)           | URL policy, identity gate by type, device credential at rest, ordinary heartbeat, origin `BlobSource` with `If-Range` resume.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Unit tests; `tests/server.rs` against a fake server; `ci/e2e_server.py` against the real server.                                                                                                                                          |
| Legacy import                           | Read-only, idempotent import of the Electron player's identity, binding, credential, command idempotency, playback flag, clock offset and verified media. Every legacy file is opened without following a link or blocking on a FIFO, and checked as an open file. `--refresh` imports again after a rollback and only ever adds executed command keys.                                                                                                                                                                                                                                                                                                                                                                                                           | `edge-server` tests (`server.rs`); real-server script.                                                                                                                                                                                    |
| Server link in the daemon               | Identity gate, then the player WebSocket with the ordinary heartbeat as fallback; server clock sampling; credential deleted only on `device_credential_invalid` or `device_credential_revoked`; bounded retry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `ci/e2e_server.py`: screen online and heartbeat visible on the screen record, revocation removes the credential.                                                                                                                          |
| Manifests (M3)                          | `GET /player/manifest` with ETag reconciliation into a persisted target; structure, screen, schema (11–15) and media-claim validation; typed incompatibility against the static WPE profile; CAS preparation from the origin; pending written only while it is the target; activation at an item boundary, after the grace period, or at once for a takeover; promotion only after the current renderer accepted exactly that activation and reported content evidence.                                                                                                                                                                                                                                                                                           | `tilecastd/tests/playback.rs` (fake server and scripted renderer, Linux); `ci/run-e2e-server.sh` with the real server and WPE.                                                                                                            |
| Media capability channel                | The renderer reads media only through `tcmedia://cap/` grants over the daemon's `media.sock`, bound to the renderer process and activation generation. The renderer unit cannot see `/var/lib/tilecast-edge`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `tests/media_channel.rs`; `renderer-wpe` C tests; the headless and real-server scenarios.                                                                                                                                                 |
| Layouts, widgets and plugins            | Layout and widget items travel as references plus a projection context; the shared Player Runtime projects them with its projection compatibility code (the code the Electron player runs) at the corrected clock. Brand Bug media resolves through a daemon alias table to granted capabilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `packages/player-runtime/src/compat/projector.test.ts`; `layout_shown` and `layout_zone_rendered` evidence in the headless and real-server scenarios.                                                                                     |
| Playback heartbeat                      | `activeManifestVersion`, `pendingManifestVersion`, `currentItemId` (its start time goes in the telemetry sample's `itemStartedAt`), `selectionSource`, playlist, schedule and takeover identifiers, `nextTransitionAt`, `lastHealthyPlaybackAt`, `lastSynchronizationError`, native presentation capabilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `tests/playback.rs`; the real-server script reads them back from Studio's assignment view.                                                                                                                                                |
| Commands (M4)                           | The ordinary command endpoint, polled at once when the verified relationship starts, every 7 s of monotonic time and on `commands.available`, by one serialized task. Records are keyed by `idempotencyKey` (migration 0003) with the delivery `id` beside it. At-most-once local execution with durable non-replay: the handler runs only after `executing` commits; a record found `executing` at start is reported as `command_interrupted` and never rerun; every redelivery of a completed key receives the stored result; results are resent until the server takes them. `restart_player_process` persists and reports `initiated` before the daemon exits. Later-milestone commands answer `unsupported_command`.                                         | `tilecastd/tests/commands.rs` (crash-point and redelivery matrix, cadence and serialization); `edge-state/tests/commands_config.rs` (migration from schemas 1 and 2); `tests/playback.rs` M4 scenarios (Linux).                           |
| Configuration (M4)                      | The ordinary configuration endpoint with `If-None-Match`, on connection, on `config.changed` and at the reconciliation interval. A document is accepted only after strict structural validation, at a supported schema and at a strictly greater `configRevision`; current and previous documents are bound to installation, screen and server, and the current one applies at start before any network access. It drives branded status surfaces, playback defaults, regional formatting in the runtime's layout and widget projection, active hours (rest surface), cache policy, status and reconciliation intervals and the recovery ladder. Refusals keep the accepted document and set `configurationError`.                                                | `player_config.rs` unit tests (Electron playback-defaults and active-hours cases); `edge-state/tests/commands_config.rs`; `tests/playback.rs` M4 scenarios (Linux).                                                                       |
| Presentation precedence (M4)            | Safe mode, then the rest surface outside active hours, then the disabled surface, then content; a takeover or Quick Present outranks rest and disabled. Safe mode now holds its surface until `exit_safe_mode`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tests/playback.rs` M4 scenarios (Linux).                                                                                                                                                                                                 |
| Bounded server bodies (M4)              | Every JSON and error body is read in chunks against an endpoint bound (64 KiB small answers, 16 KiB errors, 256 KiB configuration, 1 MiB commands, 6 MiB manifest); `Content-Length` never sizes an allocation, and an oversized error envelope is never believed as a credential rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `edge-server/tests/bounded.rs` (chunked bodies without a length) and `tests/server.rs`.                                                                                                                                                   |
| Pairing (M5)                            | A clean installation pairs with the ordinary protocol: server address from the setup surface, a discovered server or `tilecastctl pair`; URL policy, then identity with pairing enabled; bounded device metadata; the visible code on screen and in `tilecastctl status`. The poll secret and the one-time enrollment token live only in `identity/pairing-session` (0600) and are saved before enrollment, so a restart resumes and a crash after the approving poll still enrolls. Expiry and rejection start a fresh session; `pairing.reset` abandons it; a rejected credential pairs again with its server. Enrollment writes the credential, then the binding, then removes the session file. LAN discovery browses Avahi over D-Bus; results are advisory. | `tilecastd/tests/pairing.rs` (clean machine to connected, restart resume, crash with a saved token, expiry, rejection, reset, pairing disabled); `ci/e2e_server.py` pairs a fresh daemon with the real server; `discovery.rs` unit tests. |
| Capabilities                            | Provider registry with timeouts and degradation; systemd, host time sync, WPE platform, renderer and state-store capabilities.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `edge-platform` unit tests; `tests/state.rs`.                                                                                                                                                                                             |
| Installer and migration (M7)            | `tilecast-edge-migrate`: signed release install under `/opt/tilecast-edge/<version>/`, and the binding migration sequence with a durable intent record, a release self-test, an offline compatibility check, a bounded settlement on daemon status evidence, automatic rollback and boot recovery. See [`apps/edge/packaging/README.md`](../apps/edge/packaging/README.md) and the [threat-boundary review](tilecast-edge-migration-threat-review.md).                                                                                                                                                                                                                                                                                                            | `tilecast-edge-migrate` tests (crash matrix); `apps/edge/ci/run-migrate-e2e.sh` under systemd; see §7.                                                                                                                                    |
| Release delivery (M7)                   | A pinned Debian 13 builder (image digest, `snapshot.debian.org` timestamp) that builds WPE WebKit 2.54.0 from its checksummed source with Debian's own configuration, against the distribution's GLib, GStreamer and Mesa. Releases carry a signed file manifest, a CycloneDX SBOM, SHA-256 sums and a build provenance attestation.                                                                                                                                                                                                                                                                                                                                                                                                                              | `.github/workflows/edge-release.yml` (manual; rebuilds and compares every file digest).                                                                                                                                                   |
| Hardware parity (M9, software-complete) | Kernel HDMI-CEC and DDC/CI display control with readback results, scheduled display actions, the Presentation Network client of the existing `tilecast-networkd` helper, the systemd-logind idle inhibitor, device access through udev and `tilecast-display`, and the user-session bridge (§4.4 of the architecture) for audio inventory and the Noise Meter. See §9.                                                                                                                                                                                                                                                                                                                                                                                            | §9. Physical hardware is not validated (M11).                                                                                                                                                                                             |

All Rust checks pass on macOS and in the Linux image (`apps/edge/ci/test-linux.sh`). The server has no Edge-specific code.

### 1.2 What a migrated screen does today

A screen migrated with `tilecast-edge-migrate migrate`:

- keeps its player ID (the legacy `playerInstallationId`), server binding and device credential;
- verifies installation identity, holds the player WebSocket and sends the ordinary heartbeat, so Studio shows it online with its playback state;
- plays what Studio assigns (images and video, playlists, schedules, takeovers, Quick Present, Layouts, native Widgets, synchronized groups and Span panels), from its cache when the server is unreachable. Items with `stream` delivery are downloaded and verified before they play ([`tilecast-edge-capabilities.md`](tilecast-edge-capabilities.md));
- keeps the committed presentation when a manifest is invalid, a download fails or the content needs something the WPE profile cannot show (websites, YouTube, web Widgets, the Noise Meter), and reports the typed reason;
- deletes its credential when the server reports it revoked.

It also runs server commands at most once with durable non-replay and applies player configuration, live and offline (M4). A clean installation pairs without legacy state (M5).

It reports proof of play, playback failures and bounded telemetry with the Activity Event Contract v2, and answers the Studio live preview (M8). Activity waits in a bounded SQLite outbox through an outage and is delivered once; see §8.

Do not migrate a production screen before physical qualification of its hardware class (M11).

## 2. Invariants

These hold across every milestone.

1. **One credential owner.** Only `edge-server` reads the device credential, and only `AuthenticatedServer` sends it. Obtain one only through `ServerClient::verify_installation`.
2. **Identity before credential.** Normalize the URL with `url_policy::normalize_server_url`, read `/api/v1/system/identity`, require the bound installation ID, then send the credential.
3. **Credential deletion.** Delete it only on `ServerError::CredentialRejected`. Never on a network error, 5xx or `screen_disabled`.
4. **The server is the only authority.** Server state arrives only from the authenticated server. Push events wake reconciliation; they never replace it.
5. **Offline first.** Readiness and playback never wait for the server. A failed reconciliation never replaces the last good state.
6. **Bytes are verified.** Every `BlobSource` feeds the content store, which checks size and SHA-256 before promotion. Add sources; do not add a second write path.
7. **The renderer holds nothing secret.** It gets content references for objects listed in the current activation and reports evidence. Everything else is in `tilecastd`.
8. **One-way migration.** Never modify or delete legacy files. No shadow mode, no dual runtime, no second credential.
9. **Health from evidence.** Supervisor decisions use meaningful playback evidence, not process or socket liveness.

## 3. Next work

The milestones are in [`tilecast-edge.md`](tilecast-edge.md) §18.1. M1 is this foundation. The work above it is a stack of pull requests, each based on the one below:

| Layer                                                  | Scope                                                                                                                                                                          |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Player Runtime                                         | `@tilecast/player-runtime`: the shared Lit and XState display runtime that both the Electron player and `tilecast-renderer-wpe` host, with the cross-engine conformance suite. |
| M2 Server presence and M3 Manifests and content (#530) | Done: see §1.1. Exit criteria are proven by `tests/playback.rs` and the real-server script.                                                                                    |
| M4 Configuration and commands                          | See §1.1. Durable commands keyed by idempotency key, configuration reconciliation that drives playback, presentation precedence and bounded server bodies.                     |
| M5 Pairing                                             | See §1.1. A clean installation pairs, is approved and plays without legacy state.                                                                                              |
| M6 Resilience and parity; M7 Installer and migration   | See §1.1, §6 and §7.                                                                                                                                                           |
| M8 to M12                                              | One pull request per milestone, each based on the one below.                                                                                                                   |

Each later milestone uses the same three headings in its pull request description:

- **Use:** existing code to build on. Do not write a second version of it.
- **Implement:** the work.
- **Do not change:** the invariants in §2.

## 4. Known limitations

- **Linux kiosk policy:** `linuxKiosk.preventDisplaySleep` holds a systemd-logind `idle` inhibitor lock (M9). logind honors it for its idle action, and sessions that follow logind's idle hint do the same; a Wayland compositor's own idle inhibitor belongs to Wayland kiosk qualification (M11). A system without polkit refuses the lock, and `system.idle_inhibit` reports `inhibit_denied`. `fullscreenEnabled` has no effect because the renderer is always fullscreen. `power.sleepOutsideActiveHours` is an Android Power Assist setting (`apps/server/internal/settings/registry.go`); Linux players, Electron and Edge alike, show the configured off-hours surface instead. The earlier note that it waits for M9 was wrong.
- **LAN discovery** is covered by unit tests (TXT parsing and the URL policy); the CI images run no Avahi daemon, so browsing is verified on a real LAN only. Manual entry and `tilecastctl pair` are covered end to end.
- **Physical hardware:** the renderer is proven in the headless image only. DRM on real display hardware and Wayland kiosks need device validation (M11).
- **Remote web content** is not shown: websites, YouTube and web Widgets are typed incompatibilities until WPE website isolation is qualified (M11). The heartbeat reports `webRuntimeVersion: 0`, so the server refuses such assignments up front where it can.

- **Rotated Span panels:** the players do not agree on rotation (see [`tilecast-edge-capabilities.md`](tilecast-edge-capabilities.md)). Edge follows the shared runtime, as Electron does, until a rotated physical panel (M11) settles the correct result.
- **AirPlay** is not in Edge 1 (decided 2026-09-25). The AirPlay commands answer `unsupported_command`.
- **Noise Meter:** enabled in M9 through the session bridge (`noiseMeter: "host-levels"`). Measurement with real microphones is M11.
- **Reconnection after an outage (inherited, fixed in M9):** Edge retried the server after 15, 30 and 45 s and its socket after 5 to 60 s. It now uses the reference player's backoff (2 s doubling to 5 min with full jitter, the streak reset only after two healthy minutes) and reports status and reconciles on every socket open. The side-by-side parity test now measures the same missed time for both players, within 0.1 s, over the windows both screens share.
- **Live preview with the WPE renderer (inherited, fixed in M9):** the renderer crashed with SIGSEGV after a preview request. The cause was GStreamer never being initialized in the renderer's UI process (WebKit initializes it only in its web processes), and an over-released WebKit image buffer. The renderer now initializes GStreamer, captures one preview at a time under a deadline, and encodes off its main loop. A capture the renderer does not answer pauses previews (10 min, doubling to 6 h), and `renderer.preview` reports why, so a preview fault costs the preview, not playback.
- **Playback scenario timeouts (inherited, fixed in M9):** the fake server in `tests/playback.rs` waited for each new connection's first bytes in its accept loop, so one silent connection stopped it from accepting others. Classification now runs in the connection's own task with a 10 s bound; all 31 scenarios pass in CI.
- **Activity outbox bound:** the outbox keeps the newest 500 rows (at most 120 telemetry samples). A longer outage drops the oldest activity first and reports how many were dropped in an `outbox.overflow` event; the server then sees a gap, not invented playback.
- **Proof-of-play boundaries:** a session shorter than a frame at an activation boundary is reported differently by the two players: Electron attributes the restart of the outgoing item to the new presentation, Edge ignores renderer evidence for an activation it has already replaced. The side-by-side parity test ignores sessions under 250 ms for this reason.
- **Compliance after an outage (server, gbyo/tilecast#671):** the trigger `tilecast_close_sessions_after_player_restart` (server migration 00033) closes every open playback session as `unknown` on `connection.restored`, although a player that only lost the network keeps playing, and the server then records no root session until the player starts a new one. The Electron player and Edge are affected alike (about 116 s missed for both in the parity run), so compliance after any outage is understated until the server changes. The side-by-side test compares each player's confirmed root-session time over the windows both screens share, which removes the 60 s heartbeat phase from the comparison.
- **DRM output before the cutover:** the legacy display session holds DRM master, so the release self-test renders off screen (headless WPE, with the real renderer, GStreamer and runtime) and `--probe-drm` only reads the outputs. The on-screen DRM proof is the migration's settlement, and a failure rolls back. Physical DRM qualification is M11.
- **Migration output:** the migrator uses the DRM backend. A machine that must keep its desktop compositor (Edge on Wayland) cannot migrate with M7.
- **Release build:** the release workflow, the pinned builder and the WPE WebKit source build are written but have not run yet; their first run is the first release. The migration integration test uses the CI image's WPE WebKit.
- **Update activation:** WPE WebKit compiles its helper process paths in, so the private build uses the fixed prefix `/opt/tilecast-edge/current/lib/wpe`. M10 must stop the renderer before it switches `current`.

## 5. Test map

| Command                                                               | Covers                                                                                                                                                                |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cd apps/edge && cargo test --workspace`                              | All Rust unit and integration tests.                                                                                                                                  |
| `apps/edge/ci/test-linux.sh` (in `tilecast-edge-dev`)                 | The same on Linux, with fmt and clippy.                                                                                                                               |
| `apps/edge/renderer-wpe/ci/run-e2e.sh` (in `tilecast-edge-dev`)       | Daemon plus real WPE renderer scenarios.                                                                                                                              |
| `apps/edge/ci/e2e_server.py`                                          | Real server pairing, legacy import, identity gate, heartbeat, configuration, commands, fresh-installation pairing, revocation.                                        |
| `apps/edge/ci/run-e2e-server.sh` (in `tilecast-edge-e2e`)             | The same plus uploaded image and video, a Layout, offline restart, and two Edge players in one synchronized group, with the real WPE renderer.                        |
| `apps/edge/renderer-wpe/ci/run-conformance.sh`                        | Player Runtime conformance fixtures under WPE WebKit headless.                                                                                                        |
| `packages/player-runtime/conformance/compare.mjs`                     | Electron against WPE: semantic state, evidence and screenshots.                                                                                                       |
| `cd apps/edge && cargo test -p tilecast-edge-migrate`                 | Release verification and install, settlement rules, and the crash matrix: every durable transition, with and without a reboot.                                        |
| `apps/edge/ci/run-migrate-e2e.sh` (in `tilecast-edge-migrate-e2e`)    | The migrator under systemd with a real server and a lingering kiosk account: install, import failure, a crash, a power loss during settlement, acceptance.            |
| `cd apps/edge && cargo test -p tilecastd --test activity_parity`      | Edge's session tracker against the Electron player's events for every scenario in `packages/api-schema/activity/player-parity.json`.                                  |
| `npm test --workspace @gibsonmb71/tilecast-player-linux`              | The Electron side of the same file (`player-parity.test.ts`).                                                                                                         |
| `apps/edge/ci/run-activity-parity.sh` (in `tilecast-edge-parity`)     | The real Electron player and Edge side by side on one real server: a loop, a takeover, a server outage; the same proof-of-play sessions, outage flush and compliance. |
| `.github/workflows/edge-release.yml`                                  | Manual: builds, signs and packages a release, and rebuilds it to compare every file digest.                                                                           |
| `cd apps/edge && cargo test -p tilecastd --test presentation_network` | The Presentation Network client against the real `tilecast-networkd` script with a fake `nmcli`.                                                                      |
| `apps/edge/ci/run-kernel-cec.sh` (Linux host with sudo)               | The HDMI-CEC provider against vivid and `cec-follower`, with a `cec-ctl` reference.                                                                                   |
| `apps/edge/session-bridge` (`ctest`)                                  | The session bridge's frames against the IPC fixtures.                                                                                                                 |
| `.github/workflows/ci-edge.yml`                                       | All of the above on every Edge pull request, for any base branch.                                                                                                     |

An IPC protocol change needs fixtures in `packages/edge-protocol/fixtures` and tests on the Rust and C sides.

## 6. M6 qualification matrix

Each scenario is an automated test. `playback.rs` is `apps/edge/tilecastd/tests/playback.rs`: a real `tilecastd` against an in-process fake server (which can answer 503, refuse or swallow connections, break a download, answer a resume with the wrong range and drop the player WebSocket) and a scripted renderer on the real IPC socket. A killable daemon runs on its own runtime; killing it stops every task where it stands, with no shutdown path. Wall time and free space come in through `Environment`, so a test can step the clock or bring the disk to its floor.

| #   | Scenario                                  | Test                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Server unavailable at boot                | `cached_presentation_plays_after_restart_without_the_server` (503); `a_refused_or_blackholed_server_never_stalls_committed_playback` (requests that never answer); real-server script (server stopped).                                                   |
| 2   | Server disappears during playback         | `a_refused_or_blackholed_server_never_stalls_committed_playback`: connections refused, no re-presentation, link `retrying`, then recovery.                                                                                                                |
| 3   | WAN outage                                | The same test: accepted connections that never answer, a renderer restart and a daemon restart during the outage.                                                                                                                                         |
| 4   | WebSocket loss, heartbeat fallback        | `losing_the_player_socket_falls_back_to_the_heartbeat`.                                                                                                                                                                                                   |
| 5   | Interrupted download                      | `interrupted_misranged_and_tampered_downloads_never_reach_the_screen`: the partial is kept and resumed.                                                                                                                                                   |
| 6   | Invalid Range resume                      | The same test: a `206` whose range does not start at the offset.                                                                                                                                                                                          |
| 7   | Corrupted partial                         | The same test: the partial is changed on disk before the resume; only verified bytes are stored.                                                                                                                                                          |
| 8   | Corrupted verified CAS object             | `a_killed_daemon_rehashes_its_store_and_resumes_cached_playback`: same-size damage is found after an unclean stop, and the committed manifest is repaired from the origin.                                                                                |
| 9   | Daemon restart                            | `cached_presentation_plays_after_restart_without_the_server`, `restart_during_a_trial_keeps_the_committed_manifest_until_evidence`.                                                                                                                       |
| 10  | Renderer crash and restart                | `evidence_counts_only_for_the_current_activation_of_the_current_renderer`; the synchronized rejoin in `two_players_in_a_group_share_one_timeline_that_a_clock_correction_never_moves`; real-server script (renderer killed during synchronized playback). |
| 11  | Unclean daemon shutdown                   | `a_killed_daemon_rehashes_its_store_and_resumes_cached_playback` (killed twice, once with the server gone); `tests/commands.rs` crash points.                                                                                                             |
| 12  | Power-loss crash points                   | `edge-cas` `reconciliation_handles_every_crash_boundary`; `edge-state` WAL and `synchronous=FULL`; the killed-daemon test. A real power cycle is part of M11 physical qualification.                                                                      |
| 13  | Wall clock forward and back               | `wall_clock_steps_move_schedule_windows_and_nothing_else`.                                                                                                                                                                                                |
| 14  | Host NTP correction after activation      | The same test (a correction inside a window never re-presents); the runtime's `follows the shared timeline and ignores a wall-clock step`.                                                                                                                |
| 15  | Stale persisted server offset             | `a_stale_persisted_server_offset_gives_way_to_the_next_sample`; `server_link` `a_whole_second_sample_corrects_only_what_it_can_resolve`.                                                                                                                  |
| 16  | Disk near the reserve floor               | `a_full_disk_or_store_fails_preparation_and_keeps_playing`.                                                                                                                                                                                               |
| 17  | CAS at its limit                          | The same test, with the active content pinned.                                                                                                                                                                                                            |
| 18  | State database corrupt                    | `tests/daemon.rs` `corrupt_state_enters_recovery_mode_without_recreating_it`.                                                                                                                                                                             |
| 19  | Newer schema than the binary              | `a_newer_state_schema_is_refused_without_being_touched` (byte-identical database, empty log); `edge-state` `newer_schema_is_refused_not_rewritten`.                                                                                                       |
| 20  | Invalid manifest                          | `invalid_manifests_never_replace_the_committed_presentation`.                                                                                                                                                                                             |
| 21  | Failed manifest preparation               | `failed_downloads_keep_the_committed_presentation_and_recover`.                                                                                                                                                                                           |
| 22  | Rapid manifest supersession               | `a_newer_manifest_supersedes_a_slow_preparation_and_a_stale_pending`.                                                                                                                                                                                     |
| 23  | Takeover during preparation               | `a_takeover_during_preparation_outranks_it`.                                                                                                                                                                                                              |
| 24  | Quick Present replacement and restoration | `quick_present_replaces_and_then_restores_the_assignment`.                                                                                                                                                                                                |

Two defects that this qualification found are fixed in M6: a committed manifest whose object failed its re-check after an unclean stop was never downloaded again, and the socket ping's whole-second timestamp could put a synchronized player up to a second behind its group (Edge now also samples the manifest's `serverTime`, as the Electron player does, and the server sends sub-second pings).

## 7. M7 cutover matrix

The handoff requires tests for a clean install, the migration, every cutover failure and automatic rollback. `CrashPoint` in `tilecast-edge-migrate/src/migrate.rs` names every durable transition; the crash matrix (`crash_tests.rs`) runs each one twice, once as a process crash and once as a reboot, and requires exactly one working player afterwards, the legacy files unchanged, the legacy copy (with its credential file) removed, and no moment with both stacks enabled or active.

| Scenario                                                                                | Expected                                                                              | Test                                                                                  |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Signed release, tampered file, link, foreign key, Linux Player manifest, escaping path  | Only the signed release installs; `current` never points at a partial tree            | `release.rs` tests; systemd test (`setup`)                                            |
| Clean install                                                                           | Settles on the setup surface; a failure disables Edge and restores the display        | `a_clean_install_settles_on_the_setup_surface_or_restores_the_display`                |
| Self-test fails, no usable DRM output, incompatible legacy presentation, no legacy unit | Refused; nothing changed                                                              | `failures_before_the_cutover_refuse_and_touch_nothing`                                |
| Import fails (server unreachable, identity mismatch)                                    | Rolled back before Edge starts                                                        | `a_failed_import_rolls_back_before_edge_ever_starts`; systemd test (`import-failure`) |
| Settlement: no server, incompatible, frozen screen, legacy restarted                    | Rolled back with the reason                                                           | `failed_settlement_rolls_back_with_its_reason`                                        |
| Crash at each of the 28 transitions                                                     | Refused before the cutover, accepted only after `AcceptIntent`, otherwise rolled back | `every_crash_point_recovers_to_exactly_one_player`; systemd test (`crash`)            |
| Power loss during settlement                                                            | The boot recovery rolls back before the unaccepted Edge can start                     | systemd test (`start-settling`, `after-reboot`)                                       |
| Healthy migration                                                                       | Accepted after a full stable window; rollback then refused                            | `a_healthy_migration_is_accepted_only_after_settlement`; systemd test (`accept`)      |
| Migration after a rollback                                                              | Imports again with `--refresh`; no command runs twice                                 | `a_second_migration_after_a_rollback_imports_again`; `server.rs` refresh test         |
| Commands during settlement                                                              | Held on the server until acceptance or rollback                                       | `a_migration_probation_leaves_commands_on_the_server_until_it_ends`                   |

## 8. M8 proof of play, telemetry and live preview

Edge reports with the ordinary player endpoints and the [Activity Event Contract v2](activity-event-contract.md); the server has no Edge-specific route. `tilecastd/src/activity.rs` is a port of the Electron player's `PlaybackSessionTracker` and its mapping (`apps/player-linux/src/core/activity-sessions.ts`): a root presentation session per `source:presentationId:manifestVersion`, a child session per item, and the same terminal reasons. Both players run the scenarios in `packages/api-schema/activity/player-parity.json` and must produce its `expected` events exactly.

Every event and telemetry sample is written to the SQLite outbox (`edge-state` migration 0004) in the transaction that allocates its `sequence`, before anything is sent. The outbox is flushed every 30 s, when the server connection returns and at once for reliability events; a batch holds at most 200 events, and the server's per-event refusal (`Event N:`) drops only that event. Sessions still open at a crash are closed as `player_restart` at the last time the daemon was known alive. Telemetry is a 60 s sample of connection, playback, renderer, cache, storage, uptime, clock offset and display gauges.

The live preview is driven by `GET /api/v1/player/preview-session`, polled every 15 s. While a lease is active the daemon asks the renderer for a snapshot every 20 s, or at once on `captureNow`, and uploads it. Setup, pairing and safe mode are never captured.

| Scenario                                        | Expected                                                              | Test                                                                             |
| ----------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Loop, takeover, schedule, safe mode, errors     | Edge emits the Electron player's events                               | `tests/activity_parity.rs`; `player-parity.test.ts`                              |
| Server outage                                   | Proof of play is queued and delivered once when the server returns    | `proof_of_play_is_queued_through_an_outage_and_sent_once`; side-by-side parity   |
| Crash with open sessions                        | Closed as `player_restart` at the next start                          | `sessions_open_at_a_crash_are_closed_as_player_restart_at_the_next_start`        |
| Outage longer than the bound                    | The newest 500 events are kept and the drop is reported               | `a_long_outage_keeps_the_newest_500_events_and_reports_the_dropped_count`        |
| The real Electron player and Edge on one server | The same root and item sessions, endings, outage flush and compliance | `ci/run-activity-parity.sh`                                                      |
| Studio preview lease                            | A renderer JPEG within 960×540 and 500 KiB reaches Studio             | `a_studio_preview_lease_uploads_the_renderer_capture`; real-server e2e `preview` |

Fixed while building the parity test, and not Edge-specific: every takeover returned 500 because `takeoverScreens` bound an unused `$1` (`apps/server/internal/httpapi/operations.go`, `takeover_targets_integration_test.go`), and the Electron player sent fractional values in integer telemetry fields, which the server rejects (`apps/player-linux/src/core/telemetry.ts`).

## 9. M9 hardware parity

M9 is software-complete; physical qualification is M11. The reuse review ([`tilecast-edge-m9-reuse-review.md`](tilecast-edge-m9-reuse-review.md)) records which Linux facility each subsystem uses, and why the rest is Tilecast code. The capability matrix, with how each capability is detected, its states and reasons, and its test, is in [`tilecast-edge-capabilities.md`](tilecast-edge-capabilities.md) ("Hardware capabilities (M9)").

| Subsystem                              | State                                                                                                                                              | Proof                                                                                                                                                                    |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| HDMI-CEC                               | Implemented: kernel CEC UAPI through `rustix` ioctls in one audited module; power with Report Power Status readback; Active Source.                | `edge-platform` display tests; `ci/run-kernel-cec.sh` (vivid, `cec-follower`, `cec-ctl`).                                                                                |
| DDC/CI                                 | Implemented: brightness, volume and mute over i2c-dev with a Get VCP readback; ddcutil's retries and null-reply backoff.                           | `edge-platform` display tests (fake monitor). No kernel emulates a DDC/CI display, so CI has no hardware test.                                                           |
| Display commands and scheduled actions | Implemented: the eight `display_*` commands and `displayAction` schedules; `display_state_confirmed` only after readback.                          | `display_control.rs` tests; `playback.rs` (`commands_run_at_most_once_across_redelivery_and_restart`); real-server e2e (`display_unsupported` without an adapter).       |
| Device access                          | Implemented: udev rule, `tilecast-display` group, `i2c-dev` in `modules-load.d`, `DevicePolicy=closed`; the renderer cannot reach `/run/tilecast`. | `release.rs` tests; migration under systemd checks the unit properties.                                                                                                  |
| Presentation Network                   | Implemented: typed client of the unchanged helper; provision and test commands; heartbeat fields; `network.presentation_network`.                  | `tests/presentation_network.rs` against the real helper script.                                                                                                          |
| Idle inhibitor                         | Implemented: logind `idle` lock for `preventDisplaySleep`; `system.idle_inhibit`; `[dev] idle_inhibit = false` for test harnesses only.            | Reason-mapping and settings truth-table tests; the migration test checks that the packaged configuration requests the lock from a real logind.                           |
| Session bridge and audio inventory     | Implemented: user unit, path unit, lingering, sandbox without capabilities; WirePlumber inventory; `audio.*` capabilities.                         | Bridge protocol tests; the bridge against real PipeWire and WirePlumber (CI); the migration test checks the sandbox, empty capability sets and that the bridge connects. |
| Noise Meter                            | Implemented: `pipewiresrc ! audioconvert ! level` in the bridge, derived levels only, history in SQLite until acknowledged.                        | `playback.rs` Noise Meter scenario; real-server e2e `noise meter:` step (server-stored history); bridge PipeWire test with a virtual microphone.                         |

Exit criterion: "capability matrix on reference hardware". Software and CI cannot meet it. Every hardware capability waits for physical validation on the reference hardware (M11).

[package]
name = "edge-protocol"
description = "Tilecast Edge wire contracts: identifiers, canonical JSON, signed documents, capabilities and the local IPC protocol."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[features]
# Test clocks and signing helpers. Never enabled by production binaries.
test-util = []

[dependencies]
base64.workspace = true
ring.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
time.workspace = true
uuid.workspace = true

[dev-dependencies]
edge-protocol = { path = ".", features = ["test-util"] }

[lints]
workspace = true

[package]
name = "edge-protocol"
description = "Tilecast Edge wire contracts: identifiers, digests, capabilities and the local IPC protocol."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[features]
# Test clocks. Never enabled by production binaries.
test-util = []

[dependencies]
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
time.workspace = true
uuid.workspace = true

[dev-dependencies]
edge-protocol = { path = ".", features = ["test-util"] }

[lints]
workspace = true

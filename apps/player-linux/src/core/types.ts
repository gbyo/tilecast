// The manifest types live with the projection compatibility code in the shared
// Player Runtime; the player keeps this module path for its own imports.
export * from "@tilecast/player-runtime/projection";
